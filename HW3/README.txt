The code for this assignment is stored at: https://github.com/B-Kopp/CS-7641-HW3

The code was adapted from https://github.com/cmaron/CS-7641-assignments/tree/master/assignment3 to fit with my data.

It is written in Python 3.6
You need to install the following modules:
numpy `conda install -c anaconda numpy`
scipy `conda install -c anaconda scipy`
pandas `conda install -c anaconda pandas`
scikit-learn `conda install scikit-learn`
xlrd `conda install -c anaconda xlrd`
seaborn `conda install -c anaconda seaborn`
scikit-optimize `conda install -c conda-forge scikit-optimize`
kneed `conda install -c conda-forge kneed`
matplotlib `conda install -c conda-forge matplotlib`

Code to run
`python run_experiment.py --all`
`python run_experiment.py --plot`
`run_clustering.bat`
`python run_experiment.py --plot`
